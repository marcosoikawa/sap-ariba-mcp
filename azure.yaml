# azure.yaml
# Definicao do projeto para Azure Developer CLI (azd)

name: sap-ariba-mcp
metadata:
  template: custom
services:
  ariba-mcp:
    project: ./Ariba-MCP
    language: python
    host: containerapp
  ariba-agent:
    project: ./Ariba-Agent
    language: python
    host: containerapp

infra:
  provider: bicep
  path: ./infra